CREATE TABLE EXEMPLAR (
    ID_Item INT,
    Num INT,
    Disponivel BOOLEAN DEFAULT TRUE,
    PRIMARY KEY (ID_Item, Num),
    CONSTRAINT FK_Exemplar_Item FOREIGN KEY (ID_Item)
        REFERENCES ITEM_ACERVO(ID_Item)
);
