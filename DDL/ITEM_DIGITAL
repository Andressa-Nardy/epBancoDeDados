CREATE TABLE ITEM_DIGITAL (
    ID_Item SERIAL PRIMARY KEY,
    Formato VARCHAR(10),
    Link_Acesso VARCHAR(255),
    Num_Downloads INT DEFAULT 0,
    CONSTRAINT FK_ItemDigital_Item FOREIGN KEY (ID_Item)
        REFERENCES ITEM_ACERVO(ID_Item)
);
