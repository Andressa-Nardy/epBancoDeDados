CREATE TABLE ITEM_FISICO (
    ID_Item SERIAL PRIMARY KEY,
    Setor VARCHAR(50),
    Prateleira VARCHAR(20),
    CONSTRAINT FK_ItemFisico_Item FOREIGN KEY (ID_Item)
        REFERENCES ITEM_ACERVO(ID_Item)
);
